<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <h3>All chats!!</h3>

    <% for(let chat of chats){%>
    <div class="chat">
      <p>From:<%=chat.from%></p>
      <div class="msg">
        <p>Msg:<%=chat.msg%></p>
      </div>
      <p>To:<%=chat.to%></p>
      <hr />
      <%=chat.created_at.toString().split(" ")%>
    </div>
    <%}%>
  </body>
</html>
